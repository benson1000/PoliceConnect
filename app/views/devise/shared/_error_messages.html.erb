<% if resource.errors.any? %>
  <div class="container-fluid" id="error_explanation" data-turbo-cache="false">
      <% resource.errors.full_messages.each do |message| %>
        <div class="alert alert-danger">
          <%= message %>
        </div>
      <% end %>
  </div>
<% end %>
